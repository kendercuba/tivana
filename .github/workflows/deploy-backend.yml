name: Deploy Backend to Droplet

on:
  push:
    branches:
      - main
    paths:
      - "backend/**"
      - ".github/workflows/deploy-backend.yml"

jobs:
  deploy:
    name: Deploy on Droplet
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: SSH pull and restart
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          # PM2 runs from /var/www/backend (pm2 describe tivana-backend → exec cwd / script path).
          # Pulls under /root/tivana-backend never updated the process that serves api.tivana.me.
          script: |
            cd /var/www/backend
            git pull origin main
            pm2 restart tivana-backend
